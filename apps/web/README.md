# @decku/web

SvelteKit 프론트 + 토큰 API route. **M2에서 스캐폴딩** (`@sveltejs/adapter-vercel`, Supabase Auth/Realtime).

지금은 자리표시. 이 디렉터리에 들어올 것:
- 로그인(Google OAuth) · 페어링 코드 발급 UI
- `/api/realtime-token`, `/api/pairings`, `/api/devices/claim` (`+server.ts`)
- 세션 목록 / 대화 뷰 (E2EE 복호 후 렌더)
