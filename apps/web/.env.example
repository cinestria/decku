# Supabase — Settings → API. 실제 값은 .env 에 (gitignore됨, 커밋 금지).

# 🔒 비밀: JWT Secret (HS256). pairing/realtime 토큰 서명에 사용. 서버 env에만.
SUPABASE_JWT_SECRET=

# 공개: 브라우저 Supabase 클라이언트용 (M3에서 사용)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
